diff --git a/Cargo.toml b/Cargo.toml
index 572a6a3..0a68bfd 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -84,11 +84,10 @@ maths = []
 maths-nopanic = ["maths"]
 ndarray = ["dep:ndarray"]
 proptest = ["dep:proptest"]
-rand = ["dep:rand"]
+rand = []
 rkyv = ["dep:rkyv"]
 rkyv-safe = ["rkyv/validation"]
 rocket-traits = [
-    "dep:rocket",
     "std",
 ]
 rust-fuzz = ["dep:arbitrary"]
@@ -109,7 +108,6 @@ std = [
     "arrayvec/std",
     "borsh?/std",
     "bytes?/std",
-    "rand?/std",
     "rkyv?/std",
     "serde?/std",
     "serde_json?/std",
@@ -195,17 +193,6 @@ features = ["std"]
 optional = true
 default-features = false

-[dependencies.rand]
-version = "0.8"
-optional = true
-default-features = false
-
-[dependencies.rand-0_9]
-version = "0.9"
-optional = true
-default-features = false
-package = "rand"
-
 [dependencies.rkyv]
 version = "0.7.46"
 features = [
@@ -215,8 +202,8 @@ features = [
 optional = true
 default-features = false

-[dependencies.rocket]
-version = "0.5.0-rc.3"
+[disabled.dependencies.rocket]
+version = "0.5.0"
 optional = true
 default-features = false

