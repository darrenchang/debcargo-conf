diff --git a/Cargo.toml b/Cargo.toml
index 0ff20d6..63d8269 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -104,17 +104,17 @@ harness = false
 [dependencies.cfg-if]
 version = "1.0"

-[dependencies.const-random]
-version = "0.1.17"
-optional = true
+#[dependencies.const-random]
+#version = "0.1.17"
+#optional = true

-[dependencies.getrandom]
-version = "0.3.1"
-optional = true
+[dependencies.getrandom]
+version = "0.3"
+optional = true

-[dependencies.portable-atomic]
-version = "1.0.0"
-optional = true
+#[dependencies.portable-atomic]
+#version = "1.0.0"
+#optional = true

 [dependencies.serde]
 version = "1.0.117"
@@ -164,17 +164,17 @@ version = "0.9.4"

 [features]
 atomic-polyfill = [
-    "dep:portable-atomic",
+#    "dep:portable-atomic",
     "once_cell/critical-section",
 ]
-compile-time-rng = ["const-random"]
+compile-time-rng = []
 default = [
     "std",
-    "runtime-rng",
+#    "runtime-rng",
 ]
 nightly-arm-aes = []
 no-rng = []
-runtime-rng = ["getrandom"]
+runtime-rng = ["getrandom"]
 std = []

 [target.'cfg(not(all(target_arch = "arm", target_os = "none")))'.dependencies.once_cell]
