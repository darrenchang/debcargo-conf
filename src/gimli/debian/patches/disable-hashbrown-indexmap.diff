diff --git a/Cargo.toml b/Cargo.toml
index 45094ac..c7fbbde 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -11,7 +11,6 @@

 [package]
 edition = "2024"
-rust-version = "1.88"
 name = "gimli"
 version = "0.33.0"
 build = false
@@ -49,13 +49,14 @@ resolver = "2"
 [features]
 default = [
     "read-all",
-    "write",
+    "dummy",
 ]
 endian-reader = [
     "read",
     "dep:stable_deref_trait",
 ]
 fallible-iterator = ["dep:fallible-iterator"]
+dummy = ["dep:fnv"]
 read = ["read-core"]
 read-all = [
     "read",
@@ -70,8 +71,6 @@ rustc-dep-of-std = [
 std = ["stable_deref_trait?/std"]
 write = [
     "dep:fnv",
-    "dep:hashbrown",
-    "dep:indexmap",
 ]

 [lib]
@@ -98,12 +97,12 @@ version = "1.0.7"
 optional = true
 default-features = false

-[dependencies.hashbrown]
+[disabled.dependencies.hashbrown]
 version = "0.16.0"
 optional = true
 default-features = false

-[dependencies.indexmap]
+[disabled.dependencies.indexmap]
 version = "2.0.0"
 optional = true
 default-features = false
