diff --git a/Cargo.toml b/Cargo.toml
index 236b7cd..f5e1e2c 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -47,28 +47,28 @@ features = [
     "rustls",
     "platform-verifier",
     "native-tls",
-    "socks-proxy",
+#    "socks-proxy",
     "cookies",
     "gzip",
-    "brotli",
+#    "brotli",
     "charset",
     "json",
-    "multipart",
+#    "multipart",
     "_test",
     "_doc",
 ]
 
 [features]
 _doc = ["rustls?/aws-lc-rs"]
-_ring = ["rustls?/ring"]
+#_ring = ["rustls?/ring"]
 _rustls = []
 _test = []
 _tls = ["dep:rustls-pki-types"]
 _url = ["dep:url"]
-brotli = ["dep:brotli-decompressor"]
-charset = ["dep:encoding_rs"]
+#brotli = ["dep:brotli-decompressor"]
+#charset = ["dep:encoding_rs"]
 cookies = [
-    "dep:cookie_store",
+#    "dep:cookie_store",
     "_url",
 ]
 default = [
@@ -79,39 +79,39 @@ gzip = ["dep:flate2"]
 json = [
     "dep:serde",
     "dep:serde_json",
-    "cookie_store?/serde_json",
-]
-multipart = [
-    "dep:mime_guess",
-    "dep:getrandom",
+#    "cookie_store?/serde_json",
 ]
+#multipart = [
+#    "dep:mime_guess",
+#    "dep:getrandom",
+#]
 native-tls = [
     "native-tls-no-default",
-    "dep:der",
+    "dep:der",
     "_tls",
-    "native-tls-webpki-roots",
+#    "native-tls-webpki-roots",
 ]
 native-tls-no-default = [
     "dep:native-tls",
-    "dep:der",
+    "dep:der",
     "_tls",
 ]
-native-tls-webpki-roots = ["dep:webpki-root-certs"]
-platform-verifier = ["dep:rustls-platform-verifier"]
+#native-tls-webpki-roots = ["dep:webpki-root-certs"]
+#platform-verifier = ["dep:rustls-platform-verifier"]
 rustls = [
     "rustls-no-provider",
-    "_ring",
-    "rustls-webpki-roots",
+#    "_ring",
+#    "rustls-webpki-roots",
 ]
 rustls-no-provider = [
     "dep:rustls",
     "_tls",
     "_rustls",
 ]
-rustls-webpki-roots = ["dep:webpki-roots"]
-socks-proxy = ["dep:socks"]
+#rustls-webpki-roots = ["dep:webpki-roots"]
+#socks-proxy = ["dep:socks"]
 vendored = ["native-tls?/vendored"]
-win-system-proxy = ["dep:winreg"]
+#win-system-proxy = ["dep:winreg"]
 
 [lib]
 name = "ureq"
@@ -123,10 +123,10 @@ path = "examples/cureq.rs"
 required-features = [
     "rustls",
     "native-tls",
-    "socks-proxy",
+#    "socks-proxy",
     "cookies",
     "gzip",
-    "brotli",
+#    "brotli",
     "charset",
 ]
 
@@ -143,43 +143,43 @@ required-features = ["rustls"]
 [dependencies.base64]
 version = "0.22.1"
 
-[dependencies.brotli-decompressor]
-version = "5.0.0"
-optional = true
-
-[dependencies.cookie_store]
-version = "0.22"
-features = ["preserve_order"]
-optional = true
-default-features = false
-
-[dependencies.der]
-version = "0.8.0"
-features = [
-    "pem",
-    "std",
-]
-optional = true
-default-features = false
-
-[dependencies.encoding_rs]
-version = "0.8.34"
-optional = true
+#[dependencies.brotli-decompressor]
+#version = "5.0.0"
+#optional = true
+
+#[dependencies.cookie_store]
+#version = "0.22"
+#features = ["preserve_order"]
+#optional = true
+#default-features = false
+
+[dependencies.der]
+version = "0.8.0"
+features = [
+    "pem",
+    "std",
+]
+optional = true
+default-features = false
+
+#[dependencies.encoding_rs]
+#version = "0.8.34"
+#optional = true
 
 [dependencies.flate2]
 version = "1.0.30"
 optional = true
 
-[dependencies.getrandom]
-version = "0.4"
-optional = true
+#[dependencies.getrandom]
+#version = "0.4"
+#optional = true
 
 [dependencies.log]
 version = "0.4.25"
 
-[dependencies.mime_guess]
-version = "2.0.5"
-optional = true
+#[dependencies.mime_guess]
+#version = "2.0.5"
+#optional = true
 
 [dependencies.native-tls]
 version = "0.2.14"
@@ -205,10 +205,10 @@ features = ["std"]
 optional = true
 default-features = false
 
-[dependencies.rustls-platform-verifier]
-version = "0.6.0"
-optional = true
-default-features = false
+#[dependencies.rustls-platform-verifier]
+#version = "0.6.0"
+#optional = true
+#default-features = false
 
 [dependencies.serde]
 version = "1.0.138"
@@ -222,9 +222,9 @@ features = ["std"]
 optional = true
 default-features = false
 
-[dependencies.socks]
-version = "0.3.4"
-optional = true
+#[dependencies.socks]
+#version = "0.3.4"
+#optional = true
 
 [dependencies.ureq-proto]
 version = "0.6.0"
@@ -239,15 +239,15 @@ default-features = false
 [dependencies.utf8-zero]
 version = "0.8.1"
 
-[dependencies.webpki-root-certs]
-version = "1.0.0"
-optional = true
-default-features = false
+#[dependencies.webpki-root-certs]
+#version = "1.0.0"
+#optional = true
+#default-features = false
 
-[dependencies.webpki-roots]
-version = "1.0.0"
-optional = true
-default-features = false
+#[dependencies.webpki-roots]
+#version = "1.0.0"
+#optional = true
+#default-features = false
 
 [dev-dependencies.assert_no_alloc]
 version = "1.1.2"
@@ -269,6 +269,6 @@ features = [
     "derive",
 ]
 
-[target."cfg(windows)".dependencies.winreg]
-version = "0.56"
-optional = true
+#[target."cfg(windows)".dependencies.winreg]
+#version = "0.56"
+#optional = true
