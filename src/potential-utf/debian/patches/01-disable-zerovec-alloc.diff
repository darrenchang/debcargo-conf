diff --git a/Cargo.toml b/Cargo.toml
index 66c21c8..5f80e81 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -43,13 +43,12 @@ repository = "https://github.com/unicode-org/icu4x"
 alloc = [
     "serde_core?/alloc",
     "writeable/alloc",
-    "zerovec?/alloc",
 ]
 databake = ["dep:databake"]
 default = ["alloc"]
 serde = ["dep:serde_core"]
 writeable = ["dep:writeable"]
 zerovec = ["dep:zerovec"]

 [lib]
 name = "potential_utf"
@@ -70,7 +68,7 @@ version = "0.6.0"
 optional = true
 default-features = false

-[dependencies.zerovec]
+[dependencies.zerovec]
 version = "0.11.3"
 optional = true
 default-features = false
