diff --git a/Cargo.toml b/Cargo.toml
index 8bf8254..485ea5e 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -31,10 +31,10 @@ default = [
     "serde",
 ]
 oci = [
-    "dep:auditable-serde",
+#    "dep:auditable-serde",
     "dep:flate2",
     "dep:url",
-    "dep:spdx",
+    #"dep:spdx",
     "dep:serde_json",
     "serde",
 ]
@@ -59,9 +59,9 @@ path = "tests/module.rs"
 version = "1.0.58"
 default-features = false

-[dependencies.auditable-serde]
-version = "0.8.0"
-optional = true
+#[dependencies.auditable-serde]
+#version = "0.8.0"
+#optional = true

 [dependencies.clap]
 version = "4.0.0"
@@ -94,9 +94,9 @@ optional = true
 version = "1"
 optional = true

-[dependencies.spdx]
-version = "0.10.1"
-optional = true
+#[dependencies.spdx]
+#version = "0.10.1"
+#optional = true

 [dependencies.url]
 version = "2.0.0"
