diff --git a/build.rs b/build.rs
index ae7c75a..b4e9e33 100644
--- a/build.rs
+++ b/build.rs
@@ -1,5 +1,7 @@
 extern crate cc;
 
+use std::env;
+
 const SRC: &str = "apt-pkg-c/lib.cpp";
 
 fn main() {
@@ -4,6 +4,7 @@ const SRC: &str = "apt-pkg-c/lib.cpp";

 fn main() {
     println!("cargo:rerun-if-changed={SRC}");
+    println!("dh-cargo:deb-built-using=apt-pkg-c=1={}", env::var("CARGO_MANIFEST_DIR").unwrap());

     let mut build = cc::Build::new();
     build.file(SRC);
