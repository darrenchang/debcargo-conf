diff --git a/Cargo.toml b/Cargo.toml
index 032f8d9..62dd3d5 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -80,10 +80,6 @@ default = [
     "tzdb-concatenated",
     "perf-inline",
 ]
-js = [
-    "dep:wasm-bindgen",
-    "dep:js-sys",
-]
 logging = ["dep:log"]
 perf-inline = []
 serde = ["dep:serde_core"]
@@ -192,14 +188,6 @@ version = "0.1.3"
 [dev-dependencies.walkdir]
 version = "2.5.0"

-[target.'cfg(all(any(target_arch = "wasm32", target_arch = "wasm64"), target_os = "unknown"))'.dependencies.js-sys]
-version = "0.3.50"
-optional = true
-
-[target.'cfg(all(any(target_arch = "wasm32", target_arch = "wasm64"), target_os = "unknown"))'.dependencies.wasm-bindgen]
-version = "0.2.70"
-optional = true
-
 [target."cfg(any())".dependencies.jiff-static]
 version = "=0.2.23"

