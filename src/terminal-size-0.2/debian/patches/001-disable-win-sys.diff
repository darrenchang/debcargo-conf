diff --git a/Cargo.toml b/Cargo.toml
index 9f31164..24b993d 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -31,9 +31,9 @@ repository = "https://github.com/eminence/terminal-size"
 version = "0.37.0"
 features = ["termios"]
 
-[target."cfg(windows)".dependencies.windows-sys]
-version = "0.48.0"
-features = [
-    "Win32_Foundation",
-    "Win32_System_Console",
-]
+#[target."cfg(windows)".dependencies.windows-sys]
+#version = "0.48.0"
+#features = [
+#    "Win32_Foundation",
+#    "Win32_System_Console",
+#]
